# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0

# HTTP client
httpx>=0.27.0

# File uploads (VPS)
python-multipart>=0.0.9

# Database (VPS only — install on server)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# FFmpeg (system dependency — install separately)
# Tablet (Termux): pkg install ffmpeg
# Ubuntu:           apt install ffmpeg
