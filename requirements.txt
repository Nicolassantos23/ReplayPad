# Core
opencv-python>=4.9.0  # Termux: pkg install opencv-python (pré-compilado)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0

# Database (VPS only — install on server)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0    # PostgreSQL driver

# HTTP client (tablet → VPS upload)
httpx>=0.27.0

# FFmpeg (system dependency — install separately)
# Tablet (Termux): pkg install ffmpeg
# Ubuntu:           apt install ffmpeg
